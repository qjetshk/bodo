version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bodo-back-prod
    restart: always
    env_file: .env
    ports:
      - "${PORT}:${PORT}"
